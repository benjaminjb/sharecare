var id = <%= @invite_id %>;

var address = '.pending-invites-partial' ;

var alertStyle = '<%= escape_javascript( session[:alert_style] ) %>' ;
var alertLength = '<%= session[:alert_length] %>' ;
var alertType = '<%= escape_javascript( flash[:notice] ? "success" : "error" ) %>' ;
var alertMessage = '<%= escape_javascript raw( flash[:notice] || flash[:alert] ) %>' ; 
var alertPlacement = address ;


<% if @updated %>

	$('.pending-invites-panel-invite-element-'+id).remove();

	<% if @team %>

	$('.active-teams-list').append('<%= escape_javascript(render :partial => "teams/team", :locals => { :team => @team } ) %>');

	<% end %>

<% end %>

checkChildren('.pending-invites-list');
addAlert( alertStyle, alertType, alertMessage, alertPlacement, alertLength )

// if ($('.pending-invites-list li').length === 0) {
// 	$('.pending-invites').hide();
// }